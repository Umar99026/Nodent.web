name = "nodent-api"
main = "src/index.ts"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]
account_id = "d7dc5ee8b666f8267eba35bca09bcfba"

[vars]
ADMIN_KEY = "nodent-admin-2025"
FRONTEND_URL = "https://nodent.pages.dev"

# Google Sheets (optional): add in dashboard or via CLI, then redeploy.
# Plain text vars: GOOGLE_SHEETS_SPREADSHEET_ID, GOOGLE_SHEETS_TAB_NAME (e.g. "english,methods")
# Encrypted secret: GOOGLE_SERVICE_ACCOUNT_JSON = full service account JSON (private_key may use \n)
# Optional: GOOGLE_SHEETS_SUBJECT_FROM_TAB = "1" when a single tab implies subject slug.
