{
  "name": "nodent",
  "private": true,
  "engines": {
    "node": ">=20 <23"
  },
  "scripts": {
    "dev": "cd frontend && npx vite",
    "dev:api": "npx wrangler dev --port 8787",
    "dev:all": "npx concurrently \"npm run dev\" \"npm run dev:api\"",
    "build": "cd frontend && npm run build && node ../scripts/sync-frontend-dist.mjs",
    "deploy": "npm run build && npx wrangler pages deploy . --branch main"
  },
  "devDependencies": {
    "canvas": "^3.2.3",
    "concurrently": "^9.2.1",
    "pdfjs-dist": "^5.7.284",
    "wrangler": "^4.94.0"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.12.9"
  }
}
